
# range to set a range numbers 
numbers = list(range(100))
print(numbers)

sentence = ' '.join(['hola', 'mundo', 'desde', 'un', 'join'])
print(sentence)

total = sum(numbers)
mayor = max(numbers)
menor = min(numbers)
elements = len(numbers)

print(total)
